<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bit.project.eazip.mapper.member.MemberMapper">
    <insert id="insertUser" parameterType="bit.project.eazip.domain.member.MemberDTO">
        insert ignore into usertbl (id, email) values (#{id}, #{email});
    </insert>

</mapper>